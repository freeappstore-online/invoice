{
  "name": "freeinvoiceapp",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "scripts": {
    "dev": "pnpm --filter @invoice/web dev",
    "build": "pnpm --filter @invoice/web build",
    "preview": "pnpm --filter @invoice/web preview",
    "typecheck": "pnpm --filter @invoice/web exec tsc -b",
    "test": "pnpm --filter @invoice/web test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/freeappstore-online/invoice.git"
  }
}
